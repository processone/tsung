#!/usr/bin/make -f

#export DH_VERBOSE=1
export DEB_BUILD_MAINT_OPTIONS=hardening=+all

%:
	dh $@

override_dh_auto_build:
	$(MAKE)
	$(MAKE) -C docs singlehtml

# Keeping tests removed during d/rules cleanup.
override_dh_auto_test:
